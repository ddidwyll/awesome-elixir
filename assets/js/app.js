import "../css/app.css"
import "phoenix_html"
// import socket from "./socket.js"

/*import App from "../../lib/elixir_awesome_web/templates/project/index.html.svelte"

const createApp = (props) => {
  new App({
    target: document.body.querySelector("main.container"),
    hydrate: true,
    props
  })
}

window.onload = async () => {
  const res = await fetch("/json")
  if (!res || !res.ok) return
  const json = await res.json()
  // console.table(json)
  createApp({ cats: json })
}
*/
